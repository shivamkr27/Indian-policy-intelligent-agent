# LangChain ecosystem
langchain==1.3.9
langchain-groq==1.1.3
langchain-community==0.4.2
langchain-huggingface==1.2.2
langchain-chroma==1.1.0
langchain-text-splitters==1.1.2
langgraph==1.2.5
langgraph-checkpoint-sqlite==3.1.0
aiosqlite==0.22.1

# Vector DB
chromadb==1.5.9

# Embeddings + cross-encoder reranker
# torch is installed separately in Dockerfile (CPU-only build)
sentence-transformers==5.6.0

# Sparse search (BM25)
rank-bm25==0.2.2

# API server
fastapi==0.137.2
uvicorn[standard]==0.49.0
python-multipart==0.0.32

# Document parsing
pymupdf==1.27.2.3
pymupdf4llm==1.27.2.3
python-docx==1.2.0

# Structured data / SQL
pandas==3.0.3
sqlalchemy==2.0.51

# Token counting
tiktoken==0.13.0

# Retry with exponential backoff
tenacity==9.1.4

# Web search (langchain-community's DuckDuckGoSearchRun requires the "ddgs"
# package — the "duckduckgo-search" PyPI package was renamed upstream)
ddgs==9.14.4

# Environment
python-dotenv==1.2.2
